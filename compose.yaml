# version: "3.9"
# services:
#   mysql:
#     image: mysql:8.4
#     container_name: iia-mysql
#     environment:
#       - MYSQL_ALLOW_EMPTY_PASSWORD=yes
#       - MYSQL_DATABASE=iia
#     ports:
#       - "3306:3306"
#     volumes:
#       - mysql_data:/var/lib/mysql

#   redis:
#     image: redis:7.4-alpine
#     container_name: iia-redis
#     ports:
#       - "6379:6379"

# volumes:
#   mysql_data:
#     driver: local
#     name: iia-mysql-data

services:
  client:
    container_name: iia-client
    image: node:22-bullseye
    working_dir: /app
    environment:
      - HOST=0.0.0.0
      - VITE_PORT=3006
      - CHOKIDAR_USEPOLLING=1
      - COREPACK_NPM_REGISTRY=https://registry.npmmirror.com
      - NPM_CONFIG_REGISTRY=https://registry.npmmirror.com
      - CHOKIDAR_USEPOLLING=1
      - HTTP_PROXY=http://host.docker.internal:7897
      - HTTPS_PROXY=http://host.docker.internal:7897
      - NO_PROXY=localhost,127.0.0.1,.local,.docker.internal
    volumes:
      - d:/project/IIA/client:/app
      - /app/node_modules
    command: >
      sh -lc "npm config set registry https://registry.npmmirror.com &&
              corepack enable &&
              corepack prepare pnpm@10.25.0 --activate || npm i -g pnpm &&
              pnpm -v && node -v &&
              (pnpm install --frozen-lockfile || pnpm install) &&
              pnpm exec vite --host 0.0.0.0 --port 3006 --strictPort"
    ports:
      - "3006:3006"